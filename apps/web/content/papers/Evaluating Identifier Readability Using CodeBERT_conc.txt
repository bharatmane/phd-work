=== Evaluating Identifier Readability Using CodeBERT.docx ===
ain Relevance (DR) : DR evaluates how closely identifier tokens associate with domain- based vocabulary. DR = 1 n i =1 n cos?( embedding ( w i ), embedding ( D )) (4) Pronounceability (PR ) : this method estimates how simply an identifier can be defined depending on the ratio of vowels for the entire characters. PR = # vowels # characters 5 Lexical Familiarity (LF) : Measures how acquainted identifier tokens are by averaging their utilization frequency in a large natural language corpus. LF = 1 n log ? freq w i (6) Context Consistency (CC) : estimates the semantic constancy of an identifier along with closely identifiers employing embedding- driven comparison. CC = cosine embedding name , embedding neares t k names (7) Scope Appropriateness (SA) : measures either an identifier?s length is suitable comparative for the scope size. SA = OL log scop e size +1 (8) Cognitive Load Score (CLS) : estimates the cognitive exertion need for understanding an identifier by integrating familiarity, clarity, and ambiguity- relevant factors. CLS =0.4 1- MC +0.3 1- LF +0.3 AD (9) Predictability (PRED ) : estimates how probable an identifier is of producing its surrounding code context employing a stochastic language method . PRED = P name surroundin g code (10) 3. 3 . CodeBERT -based Embedding s The proposed approach used the CodeBERT language technique for extracting the contextual and semantic information of identifiers and their surrounding code framework [23] . CodeBERT is a pre - trained algorithm , a framework that is enhanced for handling the programming- based contexts efficiently. This is trained on a huge corpus of programming languages. The CodeBERT is intended for understanding the semantic source code, and it was established to be an eff e ctive technique in different software engineering ap plications . To acquire the embedding vector of identifier token series , this method transforms the token order within three encoded vectors such as (1) Segment Embeddings [EA] (2) Position embedding [EP] and (3) Token Embeddings E tk . Position embedding supports the encoder for embed ding the token for its position. CodeBERT creates an embedding vector for a token that links to its position when the token appears two times . The architecture of CodeBERT includes twelve layers of transformers, each having 768 hidden units. The encoder employs a feed-forward neural network and a multi-head attention for extract ing semantic information from the source code. This encoder uses three dissimilar encoding vectors to map a token to an m-dimensional fixed-length vector. Specifically, CodeBERT represents each token with a 768-dimensional illustration. So , every source code file is eventually signified by a matrix with dimensions d n ? d t . E(TS)= E CLS , E T 1 , E T 2 , ??., E T n , E SEP (11) Now, E[CLS] is a superior classification token embedding, which acquires the complete semantic information of the identifier and its related utilization into the source code. In conclusion, this method combines all token embeddings employing an average pooling layer for obtaining the last feature extraction H that is consequently utilized for identifier intelligibility forecast . 3. 4 . Attention-Driven Deep Learning Architecture for Identifier Readability The extracted representations are then passed into the SA- BiLSTM model for robust identifier readability classification . BiLSTM Layer: BiLSTM is an extension of traditional LSTMs. The BiLSTM layer encompasses dual separate LSTMs to analyze the data of an identifier series from dual directions [24] . LSTM is an individual variant of recurrent model , particularly intended for retain ing the long? scale relationships for solving the gradient issue in standard RNN systems . LSTM integrates a cell memory gate for monitoring values across alternating time intervals and 3 inherent gates, such as forget, input, and output gates, for handling the data flow. T he presented technique estimates the output gate ( o t ) , forget gate ( f t ) , input gate ( i t ) , input modulation gate ( g t ) , cell memory state ( c t ) and hidden state ( h t ) , for present time - step employing identifier embedding vector s ( x t-1 ) from present time - step, hidden state vector ( h t-1 ) from current time - step and the engineered identifier attributes ( r t ) . i t =? W i * x t-1 + H i * h t-1 + R i * r t + b i , f t =? W f * x t-1 + H f * h t-1 + R f * r t + b f , o t =? W o * x t-1 + H o * h t-1 + R o * r t + b o , g t =? W c * x t-1 + H c * h t-1 + R c * r t + b c , c t = f t * c t-1 + i t * g t , h t = o t * tanh c t , (12) This equation , t represents the specific time step, H , R, and W implies learned weights, b denotes the logistic and bias sigmoid process is signified through variable s ? . The LSTM captures related information of identifier series automatically, depending on the identifier-associated features constant at each time step, as shown in equation ( 12 ). Firstly, the identifier feature vect