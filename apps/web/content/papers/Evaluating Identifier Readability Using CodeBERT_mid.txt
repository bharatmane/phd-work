=== Evaluating Identifier Readability Using CodeBERT.docx ===
in equation ( 12 ). Firstly, the identifier feature vectors are given to the two self-governing multilayer perceptrons to initialize the values of cell and hidden states ( h 0 ) . c 0 = tanh init,c e , h 0 = tanh init,h (e) (13) Withi n BiLSTM , the forward LSTM estimates the hidden state vector h f t utilizing x t and h f t-1 . Also, the regressive LSTM assesses hidden state vector s h b t employing h b t-1 and x t from the opposite direction. Be fore the BiLSTM ?s terminal hidden state vector has been acquired through integrating the backward and forward hidden vectors (hb and h f t ) , it is denoted in Eq. ( 14 ). h t = h f t , h b t (14) Self?Attention Layer: The distinctive BiLSTM could not identify the f eatures essential for determining the identifier readability, depending on related utilization . To acquire text features , which are further relevant to the contextual data , the mechanism called self?attention mechanism (SAM) is along with the presented decoding algorithm . The SAM enables the given input for interact ing with itself and extract the significant attributes by allocating more weight to designate the importance of the feature. Fig. 2 shows the architecture of the self-attention mechanism. Fig. 2. Architecture of self-attention mechanism The h t acquired from the BiLSTM layer has been given to the input for the SA layer . The SAM illustrated in equations ( 15 ). ? t = tanh W w * h t + b w , ? t = exp ( ? t * ? w ) t exp ( ? t * ? w ) a= t ? t * h t , (15) Wherever b w and W w represent the bias and weight, correspondingly , that is utilized for find ing the new information of the hidden vector ( ? t ) . ? w Signifies the great dimensional context vector employed for predict ing the importance of various features in the text series that is prepared randomly in the learned jointly and training phase. Furthermore, t he softmax layer in the decoding is employed for generating an identifier dependability forecast depend on the consequential result from the SAM layer . At e very time step t , it chooses the words with great possibility and refers them to the next time step for producing the whole text caption. y t = softmax W a *a+ b a , (16) Wherever b a , W a , and y t denote the bias and the weighted matrix, respectively. Fig. 3 illustrates the model diagram of SA- BiLSTM . Fig. 3. Model diagram of SA- BiLSTM 3. 5 . Model Optimization using AdamW AdamW is an optimized variant of the Adam optimizer that decouples weight decay from the gradient update, enhancing generalization in DL models [25] . The parameters updated in AdamW are correctly declared in the given formula : m t = ? 1 m t -1 +(1- ? 1 ) g t ' ? t = ? 2 v t -1 +(1- ? 2 ) g t ' 2 m t = m t 1- ? 1 t , ? t = v t 1- ? 2 t ? t = ? t-1 - ? ? t +? m t +? ? t-1 , (17) Wherever v t and m t signify the rapidly weighted first? and second?moment estimations of the random gradient g t , correspondingly ; ? 2 and ? 1 are the re parallel decay factors , and v t and m t denote the bias?corrected forms of v t and m t . Furthermore, ? represents the base learning?rate hyper- parameter, e signifies a small positive cons ta nt presented for ensuring the numerical stability, and ? defines the strength of the decoupled weight?decay regularizer. Contrasting the original Adam method that couples L2 normalization to the gradient update, Adam W implements weight decay direct ly to the parameters. This clear decoupling separates the normalization impact from the dynamic update , resulting in further constant convergence and better simplification. Therefore, weight decay could be incorporated as a n L2 normalization term with coefficient ? in the given amplified loss function: L total ( w )= L data ( w )+ ? 2 ? w ? 2 2 , (18) Here, L data ( w ) is the original task loss. Taking the gradient of L total ( w ) and implementing a learning?rate process submits the traditional shrinkage update, w t +1 = 1- ?? w t - ? ? w L data w t , (19) That shrinks every weight by a factor of (1- ?? ), previously the data? based modification. In AdamW , the above decay term is clearly decoupled from the dynamic moment approximation, as illustrated in the last update Equation (1 9 ). This decoupling assures that normalization does not interfere with adjustable learning dynamics, leading to further constant convergence and improved simplification. Table 2 summarizes the optimized hyperparameters u tilized in the proposed model. The input sequence length is fixed at 50, while the BiLSTM is configured with three layers, 128 hidden units, and a dropout rate of 0.3 to efficiently capture bidirectional dependencies and reduce overfitting. A self-attention mechanism with four heads and an attention dimension of 128 improve contextual feature learning. The dense layer employs 64 neurons with ReLU activation for nonlinear feature mapping. Training employ s the AdamW optimizer with a weight decay of 0.01, learning rate of 0.001, batch size of 32, 100 epochs, and gradient clipping of 1.0