esigns, limiting generalization. Tang et al. [ 19 ] Current vulnerability detection methods do not fully unify code meaning and structure in a single learning framework. DL model C/C++ dataset Accuracy of 64.46%. The approach is computationally heavy and its effectiveness may drop outside benchmark datasets. Tashtoush et al. [ 20 ] The mutual influence between code readability and complexity is not well studied. SVM, NN and DTs methods. code-readabi- lity-krod dataset. Accuracy of 90.01%. Based on Java data and specific metrics, limiting generalization. 3. Proposed Framework The proposed ECRVR-MVEL method follows a methodological framework for code readability classification . Fig. 1 depicts the overall process of the ECRVR-MVEL technique . Towards that , the proposed method involves text preprocessing, embeddings using CodeBERT, weighted voting ensemble classification , optimization and explainability analysis for improving transparency and performance of model. Fig. 1. Overall process of the proposed ECRVR-MVEL approach Preprocessing: At the initial stage , preprocessing includes t okenization, r emoving comments or normalizing white space , and d etecting language and encoding sequences to clean and standardiz e raw code for increasing the model performance . Text Embeddings : CodeBERT embedding model is used to convert raw source code into vector embeddings representat io n that allows effect ive code readability assessment. Weighted M ajority Voting Ensemble Model : This voting-based ensemble model integrates GCN, DBN and BiTCN approaches to classify the code into three readability classes , namely high, medium and low levels . Parameter Tuning P rocess : The utilization of Nadam optimize r is to improve classification performance and convergence speed of the model . Explainability Model : A LIME-based explainable approach is used to visualize the transparency of the model. 3.1. Text Preprocessing This preprocess ing is a critical phase for preparing raw source code for efficient examination and subsequent application of ML and DL techniques [ 21 ] . While programming languages show semantic rules and structured syntax, traditional NLP models can be carefully adapted. In this method, C++ and Python source codes are key inputs, and a dedicated pre-processing pipeline is used for preserving syntactic integrity whereas improving performance as well as model interpretability. Tokenization : In the initial phase of preprocessing, raw source code was tokenized to order of useful tokens. Different natural language text s , a code tokenizer treats programming construct s as atomic units. Particularly, keywords (for example, while, if, return, for), identifiers (function names, class names , and variable names), literals (string and numeric constants), operators (logical, relational, and arithmetic), punctuation symbols and delimiters are obtained as separate tokens. Language-specific tokenization is used for ensuring precise parsing of C++ as well as Python syntax, thus preserv ing both structural and lexical data vital for downstream learning challenges. Comment Removal and Whitespace Normalization : Source code frequently covers irregular formatting and comments, which are unrelated to functional semantics. For noise reduction and enhancing representation constancy, multi-line as well as single-line comments are optionally eliminated in preprocessing. Moreover, redundant line breaks, excessive whitespace, and unreliable indentation are normalizer s . Although indentation has been syntactically important in Python, normalizer is made carefully to retain logical structure. This phase enhances robustness, decrease s vocabulary size, and improves learning efficacy without changing program action. Programming Language Detection : As the framework helps both C++ and Python inputs, an automatic language classification model is used previous to encod ing tokens. Language classification is obtained by utilizing file extensions, reserved keyword analysis, and syntactic patterns. It allows the selection of proper tokenization rules and encoding approaches, assuring that language-specific features are capture d accurately. Sequence Encoding : Afterward , tokenization, the extracted tokens are map ped into numerical representations appropriate for model ingestion. All tokens are encoded by employing a vocabulary created from the training corpus, and sequences are truncated or padded to a fixed length to uphold uniform input dimensions. Language-aware embedded were used for capturing syntactic dependency and semantic relations inside the code. It s encoded representation acts as a normalized and structured input for subsequent feature extractor s and classifier mechanisms. 3.2. CodeBERT-based E mbeddings Code embedded convert s into program code with incessant vector models , allowing the comprehen sion of logical relations and semantical code s [ 22 ] . For efficiently capturing the complexities of code, code embedded to solve dual crucial considerations: (1) Vulnerable code components consist of crucial elements such as conditional judgments as well as variable declarations will take together from other crucial positions. (2) Code statements inside a function were logically and consecutive ly relevant , need embedd s to maintain longer?distance dependency and uphold semantic al precision. Utilizes CodeBERT as code embedd ing , employing the multi?layer Transformer encoded by producing semantic al depict ions of source code. The multi?head self?attention model resides at the heart of CodeBERT that calculates token?level relations for capturing the variable dependency and control flow data. The model allows CodeBERT to concentrate on crucial code elements efficiently , enabling the feature extraction through different significant areas. Capturing the longer?distance relations inside the code, encode position has been integrate d into the token embedded of the input layer. To add encode position for the token vectors, the method obtains the capability of best preserving the semantic and structural features, taking logical connections through distant code statements. P E (pos,2i) = sin pos 1000 0 2i d mode1 (1) P E (pos,2i+1) = cos pos 1000 0 2i d mode1 (2) 3.3. Weighted V oting -based Ensemble Classification The weighted voting-based ensemble model integrates GCN, DBN, and Bi-TCN for categorizing code readability levels . T he decision?making in the voting approach known as the weighted majority voting ensemble model , wherein weights are decided as per the classifier outcome of all DL classification [ 23 ] . In this method, classification s that have shown high outcomes in the prior evaluations are awarded maximal weights that could support to enhance the model execution. The decision?making method employs the weight ed strategy for assuring more consistent classification has more impact on the last decision. WC = AC ?AN (3) 3.3.1. GCN In GCN, the embedded vector ization of text and image nodes are upgrade d to integrate the connection data from adjacent nodes [ 24 ] . Moreover , the weight of edge are decided by the cosine similarity among the sentences and the cropped imag eries . Therefore, this method upgrades every node?s data more visibly while the relations among texts and images are nearby. To use three layers of the GCN model; Hence, l implies the values of 1, 2, or 3 . While l=1, x i l-1 denotes the early embedded obtained via BLIP. In the final layer (l=L) , x i L denotes the ending node embedded for each node i , L is 3 for the instances. where, h s,i v depicts the ending image embeds and h s,j t denote text ure node embeds , correspondingly, here s implies the index of the subject, i signifies the index of the cropped image, and j denote the index of the sentence. Within a GCN that deliberates on the weights of edges, the relations among sentences and sub?images were learned to account for local data up to three hops. The last embedd s of all nodes exposes data from neighboring text nodes as well as neighboring image nodes. Fig. 2 denotes the structure of GCN technique . Graph?level C lassification After attained the final embedded vectors for each node, these are aggregated into a graph?level embedded vector for the graph?level categorization s . To use mean pooling, the graph?level embedded vector has the mean of each node 's embedded vectors. The global mean pooling vector for each node and for subject s , impli es h s as. h s = 1 2 h s,i v + h s,j t (4) Here, ??? implies the operation of mean, h s,i v signifies the final image embeds a vector and h s,j t denotes the text node embeds vector, correspondingly. y=Linear h s (5) Fig. 2 . Structure of GCN technique 3.3.2. DBN This network offers the initial layer of structure to make an unsupervised hierarchical feature extractor [ 25 ] . Thereby, the method contains various layers of RBM that are probabilistic NNs, which learn the model of input information throughout unsupervised training. It enables every layer to gradually abstract characteristics that in turn allow the technique for discovering intricate patterns to distinguish from applications. At th is stage, t he DBN structure comprises L stacked layers of RBM, let all layers RBM, for/=1,2, ? , L contains a visible layer v, and h denotes the equivalent latent layer. These layers were organized by the outcome of all RBM acts as the input to the succeeding layer. This configuration allows the gradual abstraction of feature models , allowing the DBN for learning gradually high?level and intricate forms of input data. The probabilistic behavior of all RBM layer has been categorized by the joint distribution among the hidden and visible units . P v, h = 1 Z ex -E v, h (6) Here, Z denote function of partition and E(v, h) signifies the energy function . E v, h =- v T Wh- b T v- c T h (7) 3.3.3. BiTCN The model is an innovative DL model structure that is intended for improving longer time series forecast [ 26 ] . This is structured into 3 major elements: feature fusion layer, residual connections and bidirectional temporal blocks. Bidirectional Temporal Blocks This model recollects dilation convolution as a main function. especially backward convolution, which is a significant element of the BiTCN, presents a further pathway, which allows the network to integrat e both past dependency and upcoming covariates. The arithmetic model of backward and forward dilated convolutions in the equation: H f t = i=0 k-1 h f i ? x t- d m ?i (8) H b t = i=0 k-1 h b i ? x t+ d m ?i (9) Here, H f (t) denotes the forward outcome BiTCN and H b (t) implies the backward outcome of BiTCN, h f (i) signifies the forward component in convolution kernel and h b (i) implies the backward component with convolution kernel, k signifies dimension of kernel, x t represents the input, and d m ?i implies expansion factor. Feature Fusion Layer This layer is intended for combining backward as well as forward outcome s into a unified representation. Its layer assures that the method efficiently utilizes data from both temporal perception , improving the feature extractor and enhancing prediction outcomes. The fused outcome H(t) could be computed in below expression. H b H t =?? H f t + 1-? ? H b t = i=0 k-1 h b i ? x t+ d m ?i (10) Residual C onnections The major impact of residual connection train to stabiliz e and enabl e flow of gradient, connecting the input exactly to the outcome of all layers. It protects disappearing gradients in deep er networks and preserves important characteristics from previous layers. The outcome y BiTCN is computed in below: y BiTCN =ReLU H t + x t (11) 3.4. Nadam -driven Performance Enha n cement Nadam (Nesterov?accelerated Adaptive Moment Estimation) is an expansion of the Adam optimization, which combines NAG into the momentum element of Adam [ 27 ] . In contrast with standard momentum upgrades, Nadam uses Nesterov?s lookahead approach where the gradient has been calculated after velocity. It enables the optimization for anticipating the additional parameters position, leading to constant upgrade and more responsive system . By incorporating the adaptive moment valuation of Adam with Nesterov momentum, it intends to enhance both convergence speed as well as the general quality of learning techniques, especially in intricate, non?convex optimizer landscapes. Particularly, m t denote 1 st moment and v t denotes 2 nd moment at time step t . m t = ? 1 ? m t-1 + 1- ? 1 ? g t (12) v t = ? 2 ? v t-1 + 1- ? 2 ? g t 2 (13) Here, ? 1 implies exponential decay rates for 1 st moment and ? 2 implies exponential decay rate for 2 nd moments, correspondingly. Bias correction has been used to both moment evaluations to account for initializer impacts: m t = m t 1- ? 1 t , v t = v t 1- ? 2 t , (14) Let, ? 1 t =( ? 1 ) t and ? 2 t =( ? 2 ) t and t denote s the number of iterations. Dissimilarly , Nadam applies a lookahead module that integrates the gradient promptly to the upgrade rule . ? t+1 = ? t -??( ? 1 ? m t + 1- ? 1 ? g t 1- ? 1 t v t +e 1 (15) where, ? t implies parameter at iteration t, ? denote s rate of learning, and e denote smaller constant for numerical constancy Nadam?s anticipatory actions support the optimi